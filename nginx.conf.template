server {
    listen ${PORT};
    server_name localhost;

    location /docs {
        root /usr/share/nginx/html;
        autoindex on;
        autoindex_exact_size off;
        autoindex_localtime on;
    }
}