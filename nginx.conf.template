server {
    listen ${PORT};
    server_name localhost;

    location / {
        root /usr/share/nginx/html/docs;
        autoindex on;
        autoindex_exact_size off;
        autoindex_localtime on;
    }
}